# MechForge AI GUI 开发日志

## 2026-03-12

### 优化：代码质量提升

**发现者**: Claude

**问题描述**:
- Ruff 检查发现大量 Python 代码风格问题（类型注解、异常链、导入等）
- 缺少项目级 Ruff 配置
- 部分 JS 文件存在尾随空格

**解决方法**:
1. **添加 `ruff.toml` 配置**:
   - 目标 Python 3.10，行宽 100
   - 排除 dist_frontend、__pycache__
   - 忽略 E402（server.py 延迟导入）、E501（提示词长行）

2. **Python 修复**:
   - `ruff check --fix` + `ruff format` 自动修复
   - 异常链：`raise HTTPException(...) from e`
   - 类型注解：`Dict`→`dict`，`Optional[X]`→`X | None`
   - `api/errors.py`: `setup_error_handlers(app: FastAPI)`
   - `build.py`: 依赖检查导入添加 `# noqa: F401`

3. **JS 修复**:
   - 移除 `services/ai-service.js`、`app/ui/particles.js`、`app/settings/status-bar.js` 中的尾随空格

**解决效果**:
- `ruff check .` 全部通过
- 代码风格统一，符合 PEP 8 和 pyupgrade 规范
- 异常链便于调试

---

### 功能接入：Daily Agent 每日知识推送系统（经验库内嵌版）

**发现者**: Qwen Code

**功能描述**:
- 从 `time/` 文件夹接入 Daily Agent 系统到主项目
- 每天自动生成机械工程领域的知识内容
- 支持 6 种内容类型：失效案例、标准速览、计算技巧、工具技巧、材料洞察、行业动态
- **仅在经验库界面显示**，不自动弹出，需用户手动点击加载

**接入步骤**:
1. **复制核心文件**:
   - `daily_agent.py` → 项目根目录（Agent核心，APScheduler定时任务）
   - `daily_feed_ui.js` → 项目根目录（前端UI，经验库内嵌版）

2. **修改 `server.py`**:
   - 导入 DailyAgent 和路由
   - 添加 `/api/daily` 路由（获取今日推送）
   - 添加 `/api/daily/refresh` 路由（手动刷新）
   - 添加 startup/shutdown 事件启动/停止 Agent
   - 注入 DailyAgent 实例使刷新端点可用
   - 添加 `/daily_feed_ui.js` 静态文件路由

3. **修改 `index.html`**:
   - 在 `experience.js` 后引入 `daily_feed_ui.js`

4. **修改 `daily_agent.py`**:
   - 添加全局 `_daily_agent_instance` 变量
   - 添加 `set_daily_agent()` 函数用于注入实例
   - 修改 `manual_refresh()` 端点真正触发内容生成

5. **重写 `daily_feed_ui.js`**:
   - 移除全局 Toast 通知
   - 移除侧边栏 FEED 图标
   - 改为在经验库面板内注入 Daily Feed 区域
   - 添加"加载今日推送"按钮（手动触发）
   - 添加"刷新"按钮（手动生成新内容）

**配置说明**:
```python
{
    "provider": "openai",          # 使用在线 API: openai / anthropic
    "model": "gpt-4o-mini",       # OpenAI 模型
    "api_key": "sk-...",          # 填写你的 API Key
    "items_per_day": 3,           # 每次生成 3 条
    "manual_mode": True,          # 手动模式（不自动触发）
}
```

**依赖安装**:
```bash
pip install apscheduler httpx
```

**功能效果**:
- **手动触发**：用户点击"⚡ 生成今日推送"按钮才执行
- **在线 API**：使用 OpenAI/Anthropic，无需本地 Ollama
- **仅在经验库界面显示**：Daily Feed 区域嵌入经验库
- **轮询等待**：点击后自动轮询等待生成完成（约 10-30 秒）
- **重新生成**：生成后可点击"⟳ 重新生成"获取新内容
- **卡片展开**：点击卡片查看详细内容

**界面流程**:
1. 打开经验库界面
2. 看到 "⚡ 生成今日推送" 大按钮
3. 点击按钮 → 显示"AI 正在生成内容..."
4. 等待 10-30 秒，自动显示 3 条知识卡片
5. 点击卡片展开查看详情

---

## 2026-03-12

### 功能添加：设置界面（支持滑动）

**发现者**: Qwen Code

**功能描述**:
- 新增设置面板，支持触摸滑动和鼠标滚轮
- 包含 API 配置、界面设置、Daily Feed 设置、知识库设置等模块
- 配置自动保存到 localStorage

**添加文件**:
- `app/settings/settings-panel.js` - 设置面板控制器

**修改文件**:
- `index.html` - 添加设置图标和设置面板 HTML
- `styles.css` - 添加设置面板样式（含滑动优化）
- `app/main.js` - 添加设置标签切换支持

**设置项**:
| 分类 | 设置项 |
|------|--------|
| API 配置 | 服务提供商、API Key、模型名称 |
| 界面设置 | 主题、语言、字体大小 |
| Daily Feed | 启用开关、推送数量、推送时间 |
| 知识库 | 路径选择、RAG 开关 |
| 关于 | 版本信息、链接 |

**滑动优化**:
- `overflow-y: auto` 支持鼠标滚轮
- `-webkit-overflow-scrolling: touch` iOS 触摸滑动
- 自定义滚动条样式
- 触摸设备增大点击区域

---

## 2026-03-10

### 优化：知识库标题区 - 工业激光刻字风格

**发现者**: Qwen Code

**问题描述**:
- 知识库标题区为普通样式，缺少工业激光刻字质感
- 需要与经验库保持一致的工业锻造风格

**解决方法**:
1. **更新知识库标题结构** (`index.html`):
   - 添加 FORGED 旋转标记
   - 添加档案编号 "ARCH-2024-KB-V2.4"
   - 更新副标题为中英双语

2. **应用工业激光刻字样式** (`css/industrial-theme.css`):
   - 主标题：Chakra Petch 工业字体，32px，字重 700
   - 激光刻字效果：冷白带极淡青晕多层阴影
   - 中文副标题：Noto Sans SC 细体，钛合金蚀刻效果
   - 热影响区渐变线：暗红 → 橙 → 青紫 → 冷青 → 暗蓝
   - 烧蚀边角装饰和档案编号标记

**解决效果**:
- 知识库标题区与经验库风格统一
- 像打开一本高温烧蚀过的工程档案
- 强化 MechForge "锻造"主题视觉识别

---

## 2026-03-10

### 优化：Experience Library 标题区 - 工业激光刻字

**发现者**: Qwen Code

**问题描述**:
- 标题区为普通等宽字体，缺少工业质感
- 没有与"锻造"主题呼应的视觉元素
- 缺少高温烧蚀过的工程档案感

**解决方法**:
1. **字体升级** (`css/industrial-theme.css`):
   - 主标题：Chakra Petch 工业字体（替代等宽字体），32px，字重 700
   - 中文副标题：Noto Sans SC 细体（300），激光刻字感
   - 导入 Google Fonts 工业字体族

2. **激光刻字效果**:
   - 主标题：冷白带极淡青晕多层阴影
     - 内层：锐利亮线（rgba(255,255,255,0.8)）
     - 中层：淡青扩散（rgba(0,212,255,0.3)）
     - 外层：极淡晕（rgba(0,212,255,0.1)）
   - 副标题：钛合金表面蚀刻效果
     - 内陷阴影 + 顶部高光
     - 极淡青晕（rgba(0,212,255,0.15)）

3. **热影响区渐变线**:
   - 暗红(#ff4757) → 橙(#ff6b35) → 青紫(#8a2be2) → 冷青(#00d4ff) → 暗蓝
   - 3px 高度，带发光阴影
   - 长度与标题区等宽（left/right: 28px）

4. **装饰元素**:
   - 左上角烧蚀边角标记（橙色模糊边框）
   - 右上角 "FORGED" 旋转标记
   - 右下角档案编号 "ARCH-2024-EXP-V2"

**解决效果**:
- 标题从"普通文字"变为"工业激光刻字"
- 像打开一本高温烧蚀过的工程档案
- 标题本身就是"被锻造过的"视觉感受

---

## 2026-03-10

### 优化：知识库背景动态元素 - 工业真实感

**发现者**: Qwen Code

**问题描述**:
- 网格线条过于均匀对称，缺少工业真实感
- 粒子效果像"梦幻星尘"，与锻造主题不符
- 缺少动态元素暗示档案库正在被维护

**解决方法**:
1. **网格升级** (`css/industrial-theme.css`):
   - 粗细不均的机加工线条（粗/中/细/烧蚀/断裂多种类型）
   - 烧蚀线条：模糊 + stroke-dasharray 断裂效果
   - 热影响区线条：暗橙色 + blur 滤镜
   - 断裂线：不连续的虚线效果

2. **金属尘埃粒子系统** (`knowledge-ui.js`):
   - 6-8 个极暗金属尘埃（替代原来的 10-15 个亮粒子）
   - 颜色：暗青灰为主（65%），铁灰（27%），暗橙（8%）
   - 运动：缓慢向下沉降（15-25秒），像冷却金属粉尘
   - 尾迹：极短（1px），几乎不可见

3. **热应力波脉冲** (`knowledge-ui.js`):
   - 每 10-15 秒从随机网格节点扩散
   - 三色脉冲：青色（65%）、橙色（20%）、暗红（15%）
   - 效果：圆形边框向外扩散，模拟热应力波
   - 意图：暗示"档案库正在被实时维护/锻造"

4. **性能优化**:
   - 面板不可见时自动停止动画
   - 使用 MutationObserver 监听面板状态
   - 粒子自动清理，避免内存泄漏

**解决效果**:
- 网格从"数字感"变为"真实机加工金属表面"
- 粒子从"梦幻星尘"变为"高温余热工业废气"
- 脉冲效果增加活感和紧张感，暗示系统正在运行

---

## 2026-03-10

### 优化：知识库 UI 冷锻工业全息风格重构

**发现者**: Qwen Code

**问题描述**:
- 原知识库 UI 为"暗黑科技"风格，纯黑背景 + 简单六边形网格
- 缺少与 MechForge "锻造"主题的视觉关联
- 配色单一，缺少层次感和工业质感

**解决方法**:
1. **创建工业风主题样式** (`css/industrial-theme.css`):
   - 深海军蓝(#05070F)到近黑的极暗渐变底色
   - 碳纤维纹理 + 锻造划痕效果（使用 noise + linear gradient）
   - 粗细不均的机加工网格线条，模拟高温烧蚀金属表面
   - 钛合金灰(#1A252F)作为卡片底色

2. **更新配色方案**:
   - 主色：冷青(#00D4FF) - 保持科技感
   - 警示色：暗红(#ff4757) + 橙色(#ff6b35) - 用于 CRITICAL/WARNING 标签
   - 辅助色：钛合金灰系 - 营造工业金属质感

3. **多层辉光效果**:
   - 内层：锐利亮线（0 0 2px）
   - 中层：扩散雾（0 0 8px）
   - 外层：极淡晕（0 0 30px）
   - 警示辉光：红色/橙色多层发光

4. **更新知识库面板** (`index.html`):
   - 添加工业风背景层和 SVG 网格
   - 新增标签过滤器（ALL/MANUAL/SPEC/CASE/CRITICAL/WARNING）
   - 更新标题为 "KNOWLEDGE FORGE"
   - 工业风格搜索按钮和输入框

5. **优化交互逻辑** (`app/knowledge/knowledge-ui.js`):
   - 添加标签过滤功能
   - 工业风格加载动画
   - 模拟数据展示不同级别的结果项
   - 悬停效果和过渡动画

**解决效果**:
- 背景从"虚空"变为有"重工业历史感"的锻造车间底板
- 强化了 MechForge 的"锻造"主题
- 多层辉光效果提升了视觉层次感和科技感
- 警示色系的引入使重要信息更加醒目

---

## 2026-03-10

### 优化：完成第三轮优化 - 新模块引入

**发现者**: Qwen Code

**问题描述**:
- index.html 缺少新开发的核心模块引用
- 需要引入虚拟列表和搜索索引模块以支持大数据展示和快速搜索

**解决方法**:
1. 更新 `index.html` 引入新模块：
   - `core/virtual-list.js` - 虚拟列表组件，支持大数据集高效渲染
   - `core/search-index.js` - 搜索索引模块，支持全文检索和快速过滤

2. 完成第三轮优化总结：
   - 11 个 JS 核心模块
   - 3 个 UI 组件（Button、Input、Card）
   - 2 个 Python 模块（Middleware、Database）
   - 完整的 Store 状态管理系统
   - 全局错误捕获和报告系统
   - SQLite 数据库支持
   - 中间件系统（日志、性能、安全、缓存）

**解决效果**:
- 新模块已正确引入，可正常使用
- 项目架构完整，具备生产环境部署能力
- 开发体验完善，测试覆盖良好

---

## 2026-03-09

### 优化：功能增强与性能优化

**发现者**: Qwen Code

**问题描述**:
- 缺少窗口状态保存功能
- 缺少前端日志系统
- CAE 工作台功能简单，缺少交互
- 缺少主题切换支持
- 粒子效果性能可优化
- 缺少前端配置持久化

**解决方法**:

1. **desktop_app.py 优化**:
   - 添加 `ConfigManager` 类管理窗口配置
   - 添加窗口状态保存与恢复（位置、大小、最大化状态）
   - 配置文件保存到 `~/.mechforge/gui_config.json`
   - 添加更多 WindowAPI 方法

2. **前端日志系统** (`core/logger.js`):
   - 支持多日志级别（DEBUG、INFO、WARN、ERROR）
   - 支持模块化日志器
   - 支持日志持久化
   - 支持日志导出

3. **CAE 工作台增强** (`app/cae/cae-workbench.js`):
   - 添加视图控制按钮（线框、实体、云图）
   - 添加鼠标交互（拖拽旋转、滚轮缩放）
   - 添加视图重置功能
   - 添加模拟求解进度显示

4. **主题切换支持** (`core/theme.js`):
   - 支持暗色/亮色主题切换
   - 主题状态持久化
   - 状态栏添加主题切换按钮

5. **粒子效果优化** (`app/ui/particles.js`):
   - 使用对象池模式复用火花元素
   - 使用 DocumentFragment 减少重排
   - 添加暂停/恢复控制

6. **前端存储模块** (`core/storage.js`):
   - 统一的本地存储管理
   - 支持命名空间
   - 支持过期时间
   - 提供便捷方法

**解决效果**:
- 窗口状态自动保存和恢复
- 前端调试更加方便
- CAE 工作台交互体验提升
- 支持主题切换
- 粒子效果性能提升
- 配置持久化更加规范

---

## 2026-03-09

### 优化：模块化架构重构

**发现者**: Qwen Code

**问题描述**:
- `server.py` 文件过长（843行），包含所有 API 端点，维护成本高
- `ai-service.js` 中 `sendMessage` 方法未传递 `message` 参数导致 API 调用失败
- 缺少统一的错误处理机制
- 前端模块初始化缺少错误边界处理
- 缺少 `pyproject.toml` 配置文件

**解决方法**:
1. 将 `server.py` 拆分为模块化 API 结构：
   - `api/__init__.py` - 模块导出
   - `api/state.py` - 全局状态管理
   - `api/deps.py` - 依赖注入（懒加载）
   - `api/health.py` - 健康检查 API
   - `api/chat.py` - 聊天 API
   - `api/rag.py` - RAG API
   - `api/config.py` - 配置 API
   - `api/gguf.py` - GGUF 模型 API
   - `api/errors.py` - 统一错误处理

2. 修复 `ai-service.js` 中的 bug

3. 添加统一错误处理中间件

4. 优化前端模块初始化

5. 添加 `pyproject.toml` 配置

**解决效果**:
- 代码结构清晰，每个模块职责单一
- API 调用正常工作
- 错误响应格式统一
- 前端初始化更加健壮
- 项目配置标准化

---

## 模板

### 问题/优化：[标题]

**发现者**: [名字]

**问题描述**:
[描述问题或优化需求]

**解决方法**:
[描述解决步骤]

**解决效果**:
[描述解决后的效果]

---